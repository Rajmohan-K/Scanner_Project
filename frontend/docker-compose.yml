version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      NEXT_PUBLIC_API_BASE_URL: http://host.docker.internal:5000
      NEXT_PUBLIC_WS_URL: ws://host.docker.internal:5000/ws
